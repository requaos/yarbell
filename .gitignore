# Nix
result
result-*

# Godot
game/.godot/
game/.import/
game/android/
export.cfg

# Build artifacts
/build/
*.apk
*.aab

# Signing keystores — never commit these. The debug keystore under keystore/ is
# tracked intentionally and is exempt below.
*.keystore
!keystore/debug.keystore

# macOS
.DS_Store
